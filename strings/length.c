// find the length of a string.
#include <stdio.h>

int main(){
    char ch[] = "welcome";
    int i;
    for(i = 0; ch[i]!='\0'; i++){

    }
    printf("Length of the string is: %d\n", i);

    return 0;
}